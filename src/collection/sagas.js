// these get imported in the middleware
// how do we want to manage sagas?
// probably we pass back a root saga with the middleware. Like middleware.runRootSaga()
// And the ability to use existing saga middleware from a project that already has saga
